import { writable } from 'svelte/store';

export const moneyboys = writable([]);
export const search = writable('');
export const boy = writable({});
export const gmSelected = writable(false);
export const gmSollessSelected = writable(false);
